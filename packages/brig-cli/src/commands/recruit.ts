import chalk from 'chalk';

export async function recruitCommand(role: string, name: string, options: any = {}) {
  console.log(chalk.yellow('🚧 Recruit command coming soon!'));
  console.log(chalk.gray('This will allow recruiting crew members from available roles'));
}