import chalk from 'chalk';

export async function treasureCommand(options: any = {}) {
  console.log(chalk.yellow('🚧 Treasure command coming soon!'));
  console.log(chalk.gray('This will view completed missions and results'));
}