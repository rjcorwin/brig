import chalk from 'chalk';

export async function ashoreCommand(island: string, options: any = {}) {
  console.log(chalk.yellow('🚧 Ashore command coming soon!'));
  console.log(chalk.gray('This will join island chat/communication'));
}