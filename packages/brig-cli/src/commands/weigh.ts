import chalk from 'chalk';

export async function weighCommand(agent?: string, options: any = {}) {
  console.log(chalk.yellow('🚧 Weigh command coming soon!'));
  console.log(chalk.gray('This will resume agents'));
}