import chalk from 'chalk';

export async function crewCommand(island?: string, options: any = {}) {
  console.log(chalk.yellow('🚧 Crew command coming soon!'));
  console.log(chalk.gray('This will list crew members'));
}