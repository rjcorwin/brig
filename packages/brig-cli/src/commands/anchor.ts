import chalk from 'chalk';

export async function anchorCommand(agent?: string, options: any = {}) {
  console.log(chalk.yellow('🚧 Anchor command coming soon!'));
  console.log(chalk.gray('This will stop/pause agents'));
}